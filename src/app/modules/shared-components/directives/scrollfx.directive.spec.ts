import { ScrollfxDirective } from './scrollfx.directive';

describe('ScrollfxDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollfxDirective();
    expect(directive).toBeTruthy();
  });
});
